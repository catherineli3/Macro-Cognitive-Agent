"""API module — FastAPI application and routes.

Entry point: main.py

Responsibilities:
- FastAPI application initialization
- Route definitions
- Middleware configuration
- Dependency injection setup

Dependencies: schemas, domain (NEVER depends on business modules directly)
"""
